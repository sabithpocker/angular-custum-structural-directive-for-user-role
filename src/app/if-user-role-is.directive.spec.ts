import { IfUserRoleIsDirective } from './if-user-role-is.directive';

describe('IfUserRoleIsDirective', () => {
  it('should create an instance', () => {
    const directive = new IfUserRoleIsDirective();
    expect(directive).toBeTruthy();
  });
});
